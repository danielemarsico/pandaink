# AGENTS.md

This file is for AI coding agents other than Claude.

**Read [`CLAUDE.md`](./.claude/CLAUDE.md) before doing any work in this repository.**
It contains the project rules you must follow. In short:

- Every commit must update `CHANGELOG.md` (Keep a Changelog format).
- All implementation tasks live in `TASKS.md` in the root folder.
- All code and code comments must be written in English.

`CLAUDE.md` is the authoritative source for these rules — defer to it for the
full and current set.
